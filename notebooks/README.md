# Notebooks

- `DeepSeek_audio_concept_generation.ipynb` is the maintained walkthrough for
  the LF/broad/contrastive candidate-generation pipeline.
- `evaluation/` contains dataset-specific CBM inspection notebooks.
- `legacy/` preserves the earlier GPT/Qwen and initial DeepSeek prototypes for
  provenance. Use the maintained CLI and walkthrough for new runs.

Run notebooks from the repository root so relative paths resolve consistently.
